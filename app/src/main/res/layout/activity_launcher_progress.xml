<?xml version="1.0" encoding="utf-8"?>
<!--
    Shown by EzQuakeLauncherActivity only while it downloads the game data
    files listed in GameDataManifest, overlaid on the same splash
    background as the theme's normal windowBackground, not a separate
    screen or dialog, so the app keeps its usual first-launch look in the
    common case (data already present, this layout is never inflated).
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:id="@+id/download_status_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:text="Downloading game data..." />

        <ProgressBar
            android:id="@+id/download_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="10000"
            android:progress="0" />

    </LinearLayout>

</FrameLayout>
